# LyraBit Frontend — environment template
# Copy as `.env.local` for development, set as build args / Railway vars for prod.

# Backend base URL (no trailing slash, no /api/v1 — services/api.ts ekliyor)
# Local:    http://localhost:5000
# Railway:  https://lyrabit-api-production-xxxx.up.railway.app
NEXT_PUBLIC_API_URL=http://localhost:5000
